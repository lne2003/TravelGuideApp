buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.6.0") // Use the latest version
        classpath("com.google.gms:google-services:4.3.15") // Google services classpath
    }
}